/*global em*/
em.number = {
    range: function(from, to, inclusive ) {

        var values, count;
        if (to >= from) {

            count = to - from;
            values = new Array(count);
            for(var i = from; i < to; i++) {

            }

        } else {

        }
    }
};


